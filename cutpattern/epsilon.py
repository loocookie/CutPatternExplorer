"""판정 종류별 허용 오차. 설계 문서 §14.

하나의 전역 epsilon을 모든 판정에 쓰지 않는다. 각 상수는 물리적 의미가
다르므로 따로 조정할 수 있어야 한다.
"""

# 법선 평행 / 동일성
NORMAL_EPS = 1e-9

# carrier 병합 격자 및 최근접 임계 (§7.4)
# 부동소수 노이즈(~1e-16)와 누적 오차보다 크고,
# 실제로 구분되는 절단면 사이 거리보다는 훨씬 작아야 한다.
MERGE_EPS = 1e-7

# 각도 구간 끝점 병합
ANGLE_EPS = 1e-9

# cap 내부/외부 판정 (§7.2)
SEL_EPS = 1e-9

# 동축 판정: 진폭 s ~= 0 (§7.2 1단계)
S_EPS = 1e-9

# 퇴화원 판정: 반지름 r ~= 0
RADIUS_EPS = 1e-9
